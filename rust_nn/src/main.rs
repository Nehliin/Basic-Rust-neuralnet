pub mod neural_net;
pub mod data_loader;
fn main() {
    println!("Hello, world!");
    /*let matrix: Vec<Vec<f32>> = vec![
        vec![1.0, 1.5, 1.2],
        vec![2.0, -3.0, 4.5],
        vec![1.1, 1.2 , -1.4]
    ];
    let right_size:Vec<f32> = vec![1.2, 2.3, 3.4];

    let calculated = ne::multiply(&matrix, &right_size);

    let calculated = match calculated {
        Ok(result) => result,
        _ => vec![],
    };
    println!("{:?}",calculated)*/
}
